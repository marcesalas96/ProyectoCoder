import { Header } from "../Header/Header";
import { CartWidget } from "../CartWidget/CartWidget";

export const NavBar = () => {
    return (
        <Header>
            <CartWidget/>
        </Header>
    )
}
